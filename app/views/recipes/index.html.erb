<div class="index clearfix">
	<h2 class="super"><%= @ingredients %>, eh?</h2>
	<% if @recipes == nil || @recipes.count == 0 %>
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			<h4>Sorry honey. Nothing's been made precisely like that. Here are a few tried-and-true combos if you're feeling stumped.<h4>
				<ul class="list-unstyled">
					<li><a href="/recipes?ingredients=bacon+and+sugar&how=and">Bacon and Sugar (together!)</a></li>
					<li><a href="/recipes?ingredients=cereal+and+butter&how=and">Cereal and Butter</a></li>
					<li><a href="/recipes?ingredients=ramen&how=and">Ramen</a></li>
					<li><a href="/recipes?ingredients=cilantro+and+lime&how=and">Cilantro and Lime</a></li>
				</ul>
			</div>
		</div>
		<% else %>
		<h3>We found <%= @recipes.count %> recipe<% if @recipes.count > 1 %>s<% end %> for you.<h3>
				<ul class="list-unstyled">
					<% @recipes.each do |recipe| %>
					<li><a href="<%= recipe_path(recipe.id)%>"><%= recipe.title %></a></li>
					<% end %>
				</ul>
				<% end %>
				&nbsp;
				<h3>Feelin' other flavors?</h3>
				<a class="btn btn-default search" href="/">Search Again</a>
				<% if @recipes == nil || @recipes.count == 0 %>
				<div class="row">
					<div class="col-md-6 col-md-offset-3">
						<p class="sidenote notice">*Be sure to use commas or 'and's between stuff, please. We wanted to make sure you could ask us for "peanut butter" or "peanut and butter".</p>
					</div>
				</div>
				<% end %> 
			</div>